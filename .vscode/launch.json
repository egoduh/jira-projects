{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Jira projects — сайт",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/server.py",
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
